const DATABASE_type = process.env.DATABASE_TYPE;
const DATABASE_USER = process.env.DATABASE_USER;
const DATABASE_PASSWORD = process.env.DATABASE_PASSWORD;
const DATABASE_CLUSTER = process.env.DATABASE_CLUSTER;
const DATABASE_OPTIONS = process.env.DATABASE_OPTIONS;
const DATABASE_URL = `${DATABASE_type}://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_CLUSTER}/${DATABASE_OPTIONS}`;

module.exports = {
  DATABASE_URL,
};
